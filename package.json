{
  "name": "udsl-monorepo",
  "version": "0.1.0",
  "private": true,
  "description": "Universal Data & State Layer (UDSL) – a framework-agnostic data orchestration SDK.",
  "author": "Your Name",
  "license": "MIT",
  "packageManager": "pnpm@9.6.0",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --parallel",
    "clean": "rimraf node_modules pnpm-lock.yaml && pnpm install",
    "lint": "turbo run lint",
    "test": "turbo run test"
  },
  "devDependencies": {
    "turbo": "^2.1.0",
    "typescript": "^5.4.5",
    "tsup": "^8.0.0",
    "rimraf": "^5.0.0"
  },
  "dependencies": {},
  "workspaces": [
    "packages/*",
    "examples/*"
  ]
}
